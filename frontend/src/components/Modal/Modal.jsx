import { createPortal } from "react-dom";

import AuthForm from "@components/AuthForm";
import "./Modal.scss";

const Modal = ({}) => {
  return (
    <div>
      <AuthForm />
    </div>
  );
};

export default Modal;
